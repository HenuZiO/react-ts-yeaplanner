import styles from './Form.module.css'

interface Props {
}

const Form = (props: Props) => {
    const {} = props
    
    return (
        <form className={styles.form}>Form</form>
    )
}

export default Form